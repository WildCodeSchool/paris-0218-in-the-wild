// Utilisation de express et express router
const router = require('../config')

// Afficher tous les livecodings
router.get('/livecoding', (req, res, next) => {

})

// Afficher un livecoding
router.get('/livecoding/livecodingId', (req, res, next) => {

})

//
module.exports = router
